import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const IsLoadingContainer = styled(Box)(() => ({
  width: "100%",
  height: "500px",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
}));
